Привет. 
Задача - построить ИТ-ландшафт из перечисленных компанентов:
+ Для всех подпапок в папке Out собрать информацию по имеющимся системам:
++ Название подпапки = название ПО
+++ Файл "название ПО".md внутри подпапки = описание общее, прочитать
+++ Файл "название ПО".shema.md внутри подпапки = описание зависимостей, прочитать
+++ Файл "название ПО".shema.md внутри подпапки = описание зависимостей, прочитать
+ В папке @integrations создать файл с mermaid-схемой взаимодействия этих систем / их компанентов

Требования к результату:
+ Что нужно получить: 
++ Нужно получить в 1 месте все требуемые развертываемого инстансы ПО и зависимых компанентов
++ Схемы должны быть цветными - придумай нужно логически выделить назначение подсистем: аналитика, телеметрия, безопасность, коммуникация, процессные движки, виртуализация и т.д. 

+ Что хотим понять схемой:
++ Сколько инстансов / каких версий для какой системы нужно.
++ Какие есть инстансы с ИТ-технологиями, которые могут жить отдельно от рассматриваемого ИТ-решения для изучения вопросов их переиспользования (важны зависимости - т.к. завязки на версии), лицензирования и санкций
Пример: 
+++ Для геосервера нужен инстанс Postgre + PostGis (вероятно, отдельный, т.к. верися PG будет тесно связана с версией геосервера)
+++ Для Luxms BI тоже нужен возможно (?) отдельный инстансы PG, KeyDB, NATS, ClickHouse и т.д. Но ClickHouse у нас и так заявлен в списке, так что возможно инстанс будет общим.

+ Чего не должно быть: 
++ Громоздкойсти и нечитаемости схемы из-за огромного числа подкомпанентов и связей. 
Не нужно отображать вообще каждый узел подсистемы. 

