# Контекст слоя 2

**Статус: не закрыт.** Решения и блокер: `платформа.md` (внизу — «Нужны ответы», 7 пунктов).

## Что уже не переспрашивать

CP×3, GeoData 1.24, CNI+NetworkPolicy без default-deny в день 1, Gateway API, HAProxy пакеты+Keepalived, local-ssd, AD, стабильные FQDN + edge-a/b, Ubuntu, Vault позже, Argo один на контур, Zabbix vs Prometheus-стек, Keycloak, пулы control-plane+worker-general.

## Блокер

Цепочка пользователя `GSLB → Anti-DDoS → WAF → балансировщик ЦОДа → K8s LoadBalancer → APISIX → Portal/BFF` не сведена с HAProxy (Task_6) и с Gateway API. Docker ответ «иное». Ubuntu без 22.04/24.04.

## Термины из диалога (не путать)

| Термин | Слой |
|---|---|
| HAProxy + VIP | край ЦОДа, :6443 и HTTP |
| Gateway API | HTTP-роутер **в** K8s (Host→Service) |
| Service | выбор **пода** |
| APISIX / SCG / BFF | прикладной шлюз; APISIX нет в `sample/` |
| NetworkPolicy | восток–запад, не WAF |
| Docker Engine | не CRI; 24.0.9 не в Prod как стандарт |

Слой 3 не начинать. Старт сессии: `../ДЛЯ-СЛЕДУЮЩЕГО-АГЕНТА.md`.
