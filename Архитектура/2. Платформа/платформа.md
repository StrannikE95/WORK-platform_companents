# 2. Платформа (landing zone)

**Статус слоя:** не закрыт. Зафиксированы кластеры, Gateway API, DNS/AD, Ubuntu, Argo, Keycloak, пулы, диски. Блокер — цепочка входа и Docker «иное». **Слой 3 не начинать.**

**Платформа** — общий пол контура: Kubernetes, вход, диски, DNS, накат. Один раз на контур.

---

## Входы из слоя 1

| Факт | Статус |
|---|---|
| Kubernetes + край на Dev, ЦОД-A, ЦОД-B; на ЦОД-C нет | **ЗАФИКСИРОВАНО** |
| 1 платформенный кластер на площадку, не stretch | **ЗАФИКСИРОВАНО** |
| VIP Keepalived только внутри ЦОДа | **ЗАФИКСИРОВАНО** |
| Зона `oreon.moskva.ru`, Dev: `dev.oreon.moskva.ru` | **ЗАФИКСИРОВАНО** |
| Уход A→B без окна (контур); смерть A — минуты, дежурный; хвост данных можно потерять | **ЗАФИКСИРОВАНО** |
| Обновления — rolling | **ЗАФИКСИРОВАНО** |

Пояснения:
+ VIP (Virtual IP) + Keepalived — механизм высокой доступности:
   + Keepalived — демон на каждой из двух HAProxy-машин, общается с соседкой по протоколу VRRP
   + Активная машина владеет VIP и отвечает на него. Выходит из строя? Пассивная машина забирает VIP за секунды.
   + Клиенты всегда стучатся в один и тот же VIP — им не важно, какая физическая машина работает

---

## 2.1. Kubernetes-кластеры

| Решение | Статус |
|---|---|
| Kubernetes **1.36.4**, kubeadm, containerd | **ЗАФИКСИРОВАНО** |
| Кластеры: 1 Dev, 1 ЦОД-A, 1 ЦОД-B | **ЗАФИКСИРОВАНО** |
| Control plane **×3** на каждом (Dev — 3 маленьких) | **ЗАФИКСИРОВАНО** |
| ControlPlaneEndpoint :6443 — VIP HAProxy **этого** ЦОДа | **ЗАФИКСИРОВАНО** (Task_6) |
| GeoData — отдельный кластер Kubernetes **1.24**, не платформенный 1.36 | **ЗАФИКСИРОВАНО** |
| Имя CNI — на реализации. CNI **обязан** исполнять NetworkPolicy. Строгий default-deny **не** в день 1 | **ЗАФИКСИРОВАНО** |
| HTTP-роутер **внутри** кластера: **Gateway API**. Имя контроллера — на реализации. Без контроллера объекты не используем | **ЗАФИКСИРОВАНО** |

Пояснения:
+ Control plane — пул нод Kubernetes (содержат поды kube-scheduler, kube-controller-manager, kube-apiserver и etcd)
+ ControlPlaneEndpoint:6443 - VIP (плавающий IP-адрес) на 6443 порту, за которым стоит пара HAProxy + Keepalived. Он балансирует трафик между control-plane нодами внутри одного ЦОДа.
+ Название CNI — плагин сети, который настраивает взаимодействие между подами при создании каждого пода:
   + Выделяет под_IP из выделенного для кластера диапазона (подсети).
   + Создаёт виртуальный интерфейс (veth-пара) на ноде и подключает сеть пода.
   + Настраивает маршрутизацию — как достучаться до других нод.
   + Может настраивать NetworkPolicy — правила файрвола (iptables/eBPF) между подами
+ NetworkPolicy — правила, кто может говорить с кем внутри кластера. По умолчанию, если NetworkPolicy не настроен, все поды могут общаться со всеми.

Gateway API → выбирает (по Host/path) → Service . Service → выбирает → поды. Это не API Gateway/BFF.

Как HAProxy (или «балансировщик ЦОДа») дотягивается до контроллера Gateway API — см. уточнения: ответ «K8s LoadBalancer» с этим не сведён.

---

## 2.2. Вход — зафиксировано частично

| Решение | Статус |
|---|---|
| Пара HAProxy **3.4.3** + Keepalived на хосте, две Linux-VM, пакеты/бинарник, не Compose | **ЗАФИКСИРОВАНО** |
| VIP: :6443 TCP к API **и** HTTP(S) | **ЗАФИКСИРОВАНО** (Task_6) |
| Kafka :9092 через этот HAProxy не публикуем | **ЗАФИКСИРОВАНО** |
| SafeLine не делит 80/443 с HAProxy на той же VM | **ЗАФИКСИРОВАНО** |

Пояснения:
+ "Kafka :9092 через этот HAProxy не публикуем"
   + Kafka использует свой нативный TCP-протокол (не HTTP). HAProxy заточен под балансировку TCP/HTTP-трафика (вход в кластер).
   + Поэтому не пробрасывают :9092 через HAProxy. Клиенты подключаются напрямую к брокерам Kafka (возможно, через service/NodePort, но не через балансировщик).
+ "SafeLine не делит 80/443 с HAProxy на той же VM "
   + Port 80/443 — это HTTP-трафик. Два процесса на одной VM не могут одновременно слушать один и тот же порт.
   + SafeLine (WAF-продукт) и HAProxy должен быть на разных виртуалках


Цепочка, записанная пользователем (прикладной HTTP), **ещё не сведена** с HAProxy и Gateway API:

```text
GSLB / Global Traffic Manager
  → Anti-DDoS
  → WAF
  → балансировщик ЦОДа
  → K8s LoadBalancer
  → APISIX
  → Portal / BFF → доменные сервисы
```

Пояснения:
+ APISIX API Gateway - open-source API Gateway, единая точка входа.
   + Обрабатывает все API-запросы с помощью плагинов:
      + Сетевая часть: Управляет роутингом (выполняет роль контроллера Gateway API).
      + Безопасность: Сам разбирает HTTP-заголовки и проверяет токены (как вы и нашли).

В `sample/` нет GSLB, Anti-DDoS, APISIX. Есть Spring Cloud Gateway.

Keepalived — зависимость без файла в `sample/`, в каталог (слой 5).

---

## 2.3. Диски

| Решение | Статус |
|---|---|
| `local-ssd`: RWO, **локальный SSD**, CSI, не NFS | **ЗАФИКСИРОВАНО** |
| Имя CSI-драйвера — на реализации | **ЗАФИКСИРОВАНО** |
| `shared-fs` (RWX) — список пуст до слоя 3 | **ЗАФИКСИРОВАНО** |
| Swift — свои диски, не CSI | **ЗАФИКСИРОВАНО** |
| Имена классов одинаковы на Dev/A/B; на Dev тома меньше | **ЗАФИКСИРОВАНО** |

---

## 2.4. DNS и переключение A→B

| Решение | Статус |
|---|---|
| CoreDNS / `cluster.local` внутри кластера | **ЗАФИКСИРОВАНО** |
| Зона `oreon.moskva.ru`, ведёт **AD** | **ЗАФИКСИРОВАНО** |
| Клиентские Prod-имена стабильные: `<сервис>.oreon.moskva.ru`; при уходе на B то же имя перенацеливают | **ЗАФИКСИРОВАНО** |
| Dev: `<сервис>.dev.oreon.moskva.ru` | **ЗАФИКСИРОВАНО** |
| Служебные имена VIP: `edge-a.oreon.moskva.ru` / `edge-b.oreon.moskva.ru`; клиентские имена переключают на них | **ЗАФИКСИРОВАНО** (принято вместе со стабильными именами; возражения не было) |

Kafka advertised listeners — эти же внешние FQDN, не Pod IP.

GSLB в цепочке 2.2 может сам крутить A/B. Стыковка с «дежурный меняет запись в AD» — уточнение ниже. TTL должен позволять «минуты»; цифру не фиксируем.

---

## 2.5. ОС и runtime

| Решение | Статус |
|---|---|
| Платформенные ноды K8s и HAProxy: **Ubuntu**, одна линейка на Dev/A/B | **ЗАФИКСИРОВАНО** семейство |
| Мажор Ubuntu 22.04 vs 24.04 | **не закрыто** — уточнение 7 |
| Острова не в этом пуле: GeoData Ubuntu 22.04; Luxms — свой список (в т.ч. MosOS) | **ЗАФИКСИРОВАНО** |
| Корпоративный MOS OS на платформенный kubeadm 1.36 не ставим, пока нет матрицы вендора | **ЗАФИКСИРОВАНО** как следствие Ubuntu |
| Docker Engine **24.0.9** не платформенный runtime и не CRI Kubernetes | **ЗАФИКСИРОВАНО** |
| Нужен ли Docker Engine в Prod (не 24.0.9) | **не закрыто** — ответ был «иное» без расшифровки |

---

## 2.6. Секреты, накат, наблюдение, идентификация

| Решение | Статус |
|---|---|
| HashiCorp Vault — **позже**, не в первом срезе платформы | **ЗАФИКСИРОВАНО** (отложено) |
| Argo CD — да. **Один на контур:** свой на Dev, свой на Prod | **ЗАФИКСИРОВАНО** |
| Prod Argo живёт в ЦОД-A (мозг) и управляет кластерами A и B. Не один Argo на Dev+Prod | **ЗАФИКСИРОВАНО** как следствие «один на контур» + два кластера Prod |
| GitLab — в ландшафте; package vs Helm — слой 3 | не этот слой |
| OTel SDK — не инстанс | **ЗАФИКСИРОВАНО** |
| Zabbix — наблюдение **инфраструктуры**, обязательно | **ЗАФИКСИРОВАНО** |
| Prometheus, Alertmanager, Grafana, Tempo — **функционал**, не замена Zabbix | **ЗАФИКСИРОВАНО** роли; состав выката — слой 3/5 |
| Falco | не отвечали; слой 3 |
| Платформенный IdP: **Keycloak**. Не Keycloak из GeoData | **ЗАФИКСИРОВАНО** |
| OTel Collector | не отвечали; пока к функционалу наблюдаемости |

**Слабое место.** Vault позже: секреты до него нельзя забыть на слое 6.

**Допущение.** Учётки могут жить в AD; Keycloak — фасад SSO. Связку AD↔Keycloak здесь не проектируем.

---

## 2.7. Пулы нод

Набор одинаковый на Dev, A и B; на Dev машины слабее.

| Пул | Статус | Роль |
|---|---|---|
| `control-plane` | **ЗАФИКСИРОВАНО** | API, etcd, scheduler; прикладные поды не сюда |
| `worker-general` | **ЗАФИКСИРОВАНО** | stateless и всё, пока нет data-пула |
| `worker-data-localdisk` | **позже** | если слой 3 положит Kafka/Postgres/OS на `local-ssd` в K8s |
| острова GeoData / Luxms | не пул платформы | свои машины |

---

## 2.8. Что не в этом слое

- Куда класть Kafka vs Postgres — слой 3.
- Зеркалирование на ЦОД-B — слой 5–6.
- Helm values — слой 6–7.
- IP-план — вне рамок.

---

## Нужны ответы (блокер слоя 2)

Пользователь дописывает сюда или в чат. Агент не выдумывает. После ответов — переписать 2.2/2.5 без вопросника, затем слой 3.

1. **«Балансировщик ЦОДа» = наша пара HAProxy + Keepalived?**  
   Да / нет, это другой продукт ЦОДа (какой).

2. **«K8s LoadBalancer»** — что имеется в виду?  
   (а) Service типа `LoadBalancer` в кластере (нужен MetalLB или аналог — второй VIP рядом с HAProxy);  
   (б) так назван вход **Gateway API** (балансировщик ЦОДа бьёт в NodePort/порт контроллера) — отдельного LoadBalancer нет;  
   (в) своё.

3. **APISIX и Gateway API** — это пункт (а) 
   (а) APISIX **и есть** контроллер Gateway API (один продукт);  
   (б) Gateway API (контроллер выберем) **и отдельно** APISIX как API Gateway;  
   (в) APISIX вместо Gateway API (тогда «Да, Gateway API» отменяем).

4. **APISIX vs Spring Cloud Gateway** из `sample/`: замена / два шлюза с разными ролями / SCG не ставим?

5. **GSLB и переключение A→B:** GSLB сам уводит трафик с A на B, а запись AD/`edge-a` — запасной ручной контур? Или GSLB только «глобально», а A/B внутри города по-прежнему щёлкает дежурный в AD?

6. **Docker «иное».** Где в Prod стоит Engine (не 24.0.9): нигде / только SafeLine / только CI-builder / SafeLine+builder / иначе.

7. **Ubuntu 22.04 LTS или 24.04 LTS** на нодах K8s и HAProxy (одна на Dev/A/B). GeoData всё равно 22.04 на острове.
