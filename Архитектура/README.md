# Архитектура развёртывания — как работаем

**Новый агент: начать с [`ДЛЯ-СЛЕДУЮЩЕГО-АГЕНТА.md`](ДЛЯ-СЛЕДУЮЩЕГО-АГЕНТА.md).** Там задача, правила пары, этап и блокер.

Цель: до разработки зафиксировать **способ развёртывания**, чтобы Dev воспроизводил ошибки Prod (тот же механизм и роль-модель, меньше ёмкость).

Слои сверху вниз. Нижний не заполняем как готовый, пока верхний не закрыт.

## Порядок слоёв

| # | Папка | Вопрос слоя | Сейчас |
|---|---|---|---|
| 1 | Модель сред | Контуры и ЦОДы | **закрыт** |
| 2 | Платформа | K8s, вход, диски, DNS | **не закрыт** (7 вопросов в конце `платформа.md`) |
| 3 | Политика размещения | Куда кладём продукт из `sample/` | ждать слой 2 |
| 4 | Эталонные топологии | Классы, не 36 схем | ждать |
| 5 | Каталог инстансов | Сколько и что шарим | ждать; не использовать архив IT-landscape |
| 6 | Способ наката | Оператор / Helm / пакеты / вендор | ждать |
| 7 | IaC | Git и overlays, не код | ждать |

Выход Task_6 (`sample2/`) — **после** слоёв 1–6.

В каждой папке слоя — `контекст.md` (статус + обрывки диалога для этого слоя).

## Легенда статусов

| Метка | Значение |
|---|---|
| **ЗАФИКСИРОВАНО** | Решение принято (Task_6, sample или ответ пользователя). Менять только если меняется постановка |
| **ПРЕДЛОЖЕНО** | Черновик с явным допущением |
| **РЕШИТЬ** / блок «Нужны ответы» | Без ответа слой не закрывать; не выдумывать |

## Источники

- Состав ПО — только `sample/`.
- `5. Каталог инстансов/IT-landscape.md` — архив Task_5, не опора.
- `Out/**` и `*.consultant.md` — на продукт, не сейчас.

## Допущения рамки

1. ЦОДы — Task_6 + слой 1 (Dev 1; Prod A активный, B тёплый standby, C копии).
2. Сеть в деталях вне рамок; три требования Task_6 (вход, диски, DNS) плюс то, что пользователь сам добавил в цепочку входа (ещё не закрыто).
3. Нагрузка не замерена — не «все фичи вендора».
4. Кастомные микросервисы / интеграционный API в `sample/` нет.
